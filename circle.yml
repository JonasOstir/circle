machine:
  node:
    version: 7.9.0
  npm:
    version: 4.5.0
  yarn:
    version: 0.23.2
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
dependencies:
  override:
    - yarn
cache_directories:
    - ~/.cache/yarn
